# only search with case sensitivity if there is mixed case
--smart-case

# follow symlinks
--follow

# search other filetypes as ruby files
--type-add
ruby=haml,rabl,builder

# search other filetypes as js files
--type-add
js=json

# ignore some dirs
--ignore-dir=.bundle
--ignore-dir=tmp
